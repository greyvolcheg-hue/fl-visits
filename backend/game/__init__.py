"""Readers of the game's own formats. Nothing here knows about HTTP or a tab.

Every module is importable on its own and every one has a CLI behind `fl.py`,
because the way a format reader is checked is by printing what it read.
"""
